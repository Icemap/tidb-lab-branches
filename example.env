SERVERLESS_CLUSTER_HOST=<SERVERLESS_CLUSTER_HOST>
SERVERLESS_CLUSTER_PORT=<SERVERLESS_CLUSTER_PORT>
SERVERLESS_CLUSTER_USERNAME=<SERVERLESS_CLUSTER_USERNAME>
SERVERLESS_CLUSTER_PASSWORD=<SERVERLESS_CLUSTER_PASSWORD>
SERVERLESS_CLUSTER_DATABASE_NAME=<SERVERLESS_CLUSTER_DATABASE_NAME>
SERVERLESS_CLUSTER_ID=<SERVERLESS_CLUSTER_ID>  # optional; if omitted the first cluster returned by the API is used

TIDBCLOUD_ACCESS_TOKEN=<TIDBCLOUD_ACCESS_TOKEN>  # preferred auth; falls back to digest auth when empty
TIDBCLOUD_PUBLIC_KEY=<TIDBCLOUD_PUBLIC_KEY>
TIDBCLOUD_PRIVATE_KEY=<TIDBCLOUD_PRIVATE_KEY>
TIDBCLOUD_PROJECT_ID=<TIDBCLOUD_PROJECT_ID>  # required for branch admin user creation
